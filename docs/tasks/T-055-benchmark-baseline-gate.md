# T-055 — Benchmark Baseline Regression Gate

**Goal:** make the committed 12-case mock benchmark baseline an explicit CI
release gate.

**Scope:** benchmark regression test, CI job separation, build and CLI smoke
checks, and completion report.

**Non-goals:** no Agent runtime, Provider, policy, benchmark-case or scoring
semantic changes.

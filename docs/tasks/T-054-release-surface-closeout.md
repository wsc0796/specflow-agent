# T-054 — v1.0.1 Release Surface Closeout

**Goal:** align the GitHub release page and current project handoff with the
published `main` and `v1.0.1` state.

**Scope:** release page, current handoff, roadmap status and completion report.

**Non-goals:** no runtime, Agent, provider, policy, API or benchmark changes.

# Copy this file to a local .env only if your launcher loads it.
# SpecFlow itself reads these values from the process environment.

SPECFLOW_LLM_BASE_URL=
SPECFLOW_LLM_API_KEY=
SPECFLOW_LLM_MODEL=
SPECFLOW_LLM_TIMEOUT_SECONDS=60
